# local dev / pipeline scripts (kept locally, not deployed)
dev/

# heavy training data — lives on LUMI, regenerable
dataset/
dataset.zip
runs/
*.pt
*.cache

# old/extra onnx (keep only the deployed model)
yolo11*.onnx

# local junk / secrets
.ipynb_checkpoints/
__pycache__/
*.pyc
.DS_Store
.env
.env.local
*service*key*
